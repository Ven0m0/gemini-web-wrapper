[project]
name = "affine-shared"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = ["pydantic>=2.0.0"]

[dependency-groups]
dev = ["ruff>=0.15.21"]

[tool.uv.sources]
affine-shared = { workspace = true }
