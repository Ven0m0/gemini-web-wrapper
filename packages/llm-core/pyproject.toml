[project]
name = "affine-llm-core"
version = "0.1.0"
dependencies = ["httpx>=0.24.0", "affine-shared"]

[dependency-groups]
dev = ["ruff>=0.6.0"]

[tool.uv.sources]
affine-shared = { workspace = true }
