{
  "name": "@affine/workspace",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Affine AI Coding Workstation - Monorepo",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "bun run dev:web & bun run dev:api",
    "dev:web": "cd apps/web && bun run dev",
    "dev:api": "cd apps/api && PYTHONPATH=src:../../packages/config/src uv run uvicorn affine.api.server:app --reload",
    "dev:all": "bun run dev",
    "build": "cd apps/web && bun run build",
    "build:api": "cd apps/api && PYTHONPATH=src:../../packages/config/src uv run python -m compileall src",
    "typecheck": "cd apps/web && bun run typecheck && cd ../api && PYTHONPATH=src:../../packages/config/src uv run pyrefly check",
    "lint": "cd apps/web && bun run lint && cd ../api && PYTHONPATH=src:../../packages/config/src uv run ruff check",
    "format": "cd apps/web && bun run format && cd ../api && PYTHONPATH=src:../../packages/config/src uv run ruff format",
    "test": "cd apps/web && bun run test && cd ../api && PYTHONPATH=src:../../packages/config/src uv run pytest"
  },
  "trustedDependencies": [
    "@affine/web"
  ],
  "devDependencies": {
    "typescript": "^6.0.2"
  },
  "packageManager": "bun@1.3.11"
}
